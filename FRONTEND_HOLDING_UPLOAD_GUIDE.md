# 前端持仓上传功能使用指南

## 功能说明

前端已新增持仓 Excel 上传功能，用户可以通过网页界面直接上传持仓数据，批量更新股票的参考持股数量和成本价。

## 使用步骤

### 1. 访问页面

启动前端应用后，在页面右上角可以看到两个按钮：
- **导入持仓**（绿色按钮，带表格图标）
- **识别账单**（蓝色按钮，带相机图标）

点击 **"导入持仓"** 按钮进入持仓上传页面。

### 2. 上传文件

#### 方式一：拖拽上传
将 Excel 文件直接拖拽到上传区域

#### 方式二：点击选择
点击上传区域，从文件选择器中选择 Excel 文件

### 3. 确认上传

文件选择后，会显示文件名和大小信息。点击 **"上传并更新"** 按钮开始处理。

### 4. 查看结果

上传成功后，会显示：
- ✅ 成功更新的记录数
- ❌ 失败的记录数（如果有）
- 📋 详细的股票列表，包括：
  - 股票代码
  - 股票名称
  - 参考持股数量
  - 成本价

## Excel 文件格式要求

### 必需列

| 列名 | 可能的变体 | 说明 |
|------|-----------|------|
| 证券代码 | 证券代码、股票代码、代码 | 支持 sh.600000 或 600000 格式 |

### 可选列

| 列名 | 可能的变体 | 说明 |
|------|-----------|------|
| 参考持股 | 参考持股、持股数量、持仓数量、数量 | 数字，支持小数 |
| 成本价 | 成本价、持仓成本、成本价格、买入成本 | 数字，支持小数 |
| 证券名称 | 证券名称、股票名称、名称 | 用于创建新股票时 |

### 示例

```
| 证券代码 | 证券名称 | 参考持股 | 成本价 |
|---------|---------|---------|--------|
| sh.600000 | 浦发银行 | 1000 | 10.50 |
| sz.000001 | 平安银行 | 2000 | 12.30 |
| 600036 | 招商银行 | 1500 | 35.80 |
```

## 匹配逻辑

系统会按以下顺序查找股票：

1. **通过 symbol 匹配**：提取纯数字部分（如 sh.600000 → 600000），在 stocks 表的 symbol 字段中查找
2. **通过 code 匹配**：如果 symbol 未找到，尝试用完整代码在 code 字段中查找
3. **从 stock_basic 表获取**：如果都不存在，从 stock_basic 表查询获取完整信息
4. **创建新记录**：如果以上都不存在，创建新的 Stock 记录

## 注意事项

1. **文件格式**：仅支持 .xls 和 .xlsx 格式
2. **数据合并**：已存在的股票只会更新 reference_shares 和 cost_price 字段
3. **错误处理**：单行失败不影响其他行的处理
4. **返回主页**：点击"返回列表"按钮可返回股票列表页面

## 技术实现

### 前端组件

- **HoldingUpload.tsx**: 持仓上传组件
  - 使用 react-dropzone 实现拖拽上传
  - 调用后端 `/api/trades/upload-holdings` 接口
  - 显示详细的上传结果和股票列表

### 后端 API

- **TradeRecordController.uploadHoldingsFile()**: 处理持仓上传请求
- **HoldingExcelService.parseAndUpdateHoldings()**: 解析 Excel 并更新数据

### 数据库

- **stocks 表字段**：
  - `reference_shares`: DECIMAL(16,2) - 参考持股数量
  - `cost_price`: DECIMAL(10,4) - 成本价
  - `idx_stock_symbol`: INDEX - symbol 字段索引

## 版本更新

- **v1.4.0**: 新增持仓上传功能
  - 添加 HoldingUpload 组件
  - 优化导航栏，支持两种上传模式切换
  - 改进用户体验，显示详细的上传结果
